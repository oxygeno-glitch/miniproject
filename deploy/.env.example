# 이 파일을 /opt/project_backend/.env 로 복사한 뒤 실제 값으로 채우세요.
# .env 파일 자체는 git에 커밋하지 말고, 서버에서 chmod 600 으로 보호하세요.

DB_URL=jdbc:postgresql://localhost:5432/project
DB_USERNAME=studiog
DB_PASSWORD=CHANGE_ME

# 최초 배포 시 update로 테이블 자동 생성, 이후 안정화되면 validate 권장
DDL_AUTO=update
